{
  "name": "digital-vault-2.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "mobile": "cd mobile && npm start",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web"
  }
}
